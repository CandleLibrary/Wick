/**[API]:testing
 *
    Containers with data attribute bindings that resolve to a
    const model set, an array literal of object literals that
    contain no variable references, should be compiled into a
    static container.


    This will probably require currying/partials to be
    implemented #(5).

    This will also requires a reference graph per binding
*/
