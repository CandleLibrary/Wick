/**[API]:testing
 *
    Specifiers have component name class appended to them 
    for all components. This excludes body class names, and
    root, which is just transformed to the component class
    name.
*/

