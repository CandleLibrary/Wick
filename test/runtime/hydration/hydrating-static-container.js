/**[API]:testing-component-data
 *
 *  A Runtime component should hydrate static containers and
 *  connect components to the container element's sub-elements
 *  and add them to the runtime container instance's active
 *  components.
 */
