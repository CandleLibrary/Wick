/**[API]:testing-component-data
 *
 *  Component should hydrate static content, adding binding
 *  connections where appropriate and act no different then
 *  if the component runtime generated the DOM tree
 *  automatically.
 */
